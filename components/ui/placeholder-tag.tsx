// Renders the visible "replace before launch" marker used on every
// placeholder testimonial, case study, and gallery item. See
// content/placeholders/README.md for the full list of what needs replacing.
export function PlaceholderTag() {
  return <span className="placeholder-tag">Placeholder</span>;
}

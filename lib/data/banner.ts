// Central config for the dismissible announcement banner. Change the message
// here — no need to touch the component. Bump `id` whenever the message
// changes so it re-appears for visitors who already dismissed an older one.

export const announcementBanner = {
  enabled: true,
  id: "banner-2026-01-projects-open",
  message: "Now accepting new website & hosting projects.",
  ctaLabel: "Start a Project",
  ctaHref: "/contact",
};
